# حسابات الاختبار — Kiddy Link

> **آخر تحديث:** 21 يوليو 2026

---

## الحسابات الثلاثة (الأكثر استخداماً)

**المديرة**  
اسم المستخدم: admin  
كلمة المرور: Admin@123  

**المعلمة**  
اسم المستخدم: p2teacher  
كلمة المرور: Test@123456  

**ولي الأمر**  
اسم المستخدم: p2parent  
كلمة المرور: Test@123456  

**الرابط:** http://localhost:8082/login

---

## المديرة

| المستخدم | كلمة المرور |
|----------|-------------|
| `admin` | `Admin@123` |

---

## حسابات المراحل (كلمة المرور: `Test@123456`)

| المستخدم | الدور | الطالب | ماذا تختبر |
|----------|-------|--------|------------|
| `p2teacher` | معلمة | Phase2 Student | رفع صور، حضور، وجبات |
| `p2parent` | ولي أمر | Phase2 Student | ألبوم، حضور، وجبات، إشعارات |
| `p3teacher` | معلمة | Phase3 Student | واجبات، ملصقات |
| `p3parent` | ولي أمر | Phase3 Student | واجبات، ملصقات |
| `p4teacher` | معلمة | Phase4 Student | درdشة |
| `p4parent` | ولي أمر | Phase4 Student | درdشة |
| `p5teacher` | معلمة | Phase5 Student | بث مباشر (Agora) |
| `p5parent` | ولي أمر | Phase5 Student | مشاهدة بث |

---

## حسابات Seed القديمة

| المستخدم | كلمة المرور | الدور |
|----------|-------------|-------|
| `teacher1` | `Teacher@123` | معلمة |
| `parent1` | `Parent@123` | ولي أمر |

---

## روابط

| الخدمة | URL |
|--------|-----|
| التطبيق | http://localhost:8082/login |
| API | http://localhost:3000/api |

> استخدم **localhost** دائماً — لا تخلط مع `127.0.0.1`.

### إذا حساب p5 لا يعمل

```powershell
E:\Eman Project\scripts\ensure-test-accounts.ps1
```

---

## تدفق الصور (مهم)

```
p2teacher → رفع صورة
admin     → موافقة الصور → موافقة
p2parent  → ألبوم الصور (تظهر بعد الموافقة فقط)
```
